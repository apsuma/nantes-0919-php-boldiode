{% extends 'layout.html.twig' %}
{% block content %}
    <h1>Liste des chambres à éditer</h1>
    <p>
    {% for room in roomList %}
        <a href="edit/{{ room.id }}">{{ room.name }}</a><br>
    {% endfor %}
    </p>
{% endblock %}